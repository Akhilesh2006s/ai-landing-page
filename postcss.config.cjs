// Local PostCSS config for this project.
// We don't use Tailwind here, so keep plugins empty to avoid
// picking up the global E:\postcss.config.mjs that requires tailwindcss.

module.exports = {
  plugins: [],
};

